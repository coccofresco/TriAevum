diff --git a/tools/oot3d/native_game_runtime/oot3d_native_pica_visual_frame.cpp b/tools/oot3d/native_game_runtime/oot3d_native_pica_visual_frame.cpp
index 79eab1f07..81bbe4a55 100644
--- a/tools/oot3d/native_game_runtime/oot3d_native_pica_visual_frame.cpp
+++ b/tools/oot3d/native_game_runtime/oot3d_native_pica_visual_frame.cpp
@@ -239,7 +239,25 @@ bool ContinuousFragmentUniformValuesDiffer(
            previous.FragmentShader.Uniforms.FogColor !=
                current.FragmentShader.Uniforms.FogColor ||
            previous.FragmentShader.Uniforms.FogLut !=
-               current.FragmentShader.Uniforms.FogLut;
+               current.FragmentShader.Uniforms.FogLut ||
+           previous.FragmentShader.Uniforms.Lighting.GlobalAmbient !=
+               current.FragmentShader.Uniforms.Lighting.GlobalAmbient ||
+           previous.FragmentShader.Uniforms.Lighting.Specular0 !=
+               current.FragmentShader.Uniforms.Lighting.Specular0 ||
+           previous.FragmentShader.Uniforms.Lighting.Specular1 !=
+               current.FragmentShader.Uniforms.Lighting.Specular1 ||
+           previous.FragmentShader.Uniforms.Lighting.Diffuse !=
+               current.FragmentShader.Uniforms.Lighting.Diffuse ||
+           previous.FragmentShader.Uniforms.Lighting.Ambient !=
+               current.FragmentShader.Uniforms.Lighting.Ambient ||
+           previous.FragmentShader.Uniforms.Lighting.Position !=
+               current.FragmentShader.Uniforms.Lighting.Position ||
+           previous.FragmentShader.Uniforms.Lighting.SpotDirection !=
+               current.FragmentShader.Uniforms.Lighting.SpotDirection ||
+           previous.FragmentShader.Uniforms.Lighting.Attenuation !=
+               current.FragmentShader.Uniforms.Lighting.Attenuation ||
+           previous.FragmentShader.Uniforms.Lighting.LutTables !=
+               current.FragmentShader.Uniforms.Lighting.LutTables;
 }

 bool ContinuousViewportValuesDiffer(
@@ -921,6 +939,29 @@ void InterpolateVisualFragmentUniforms(
                                   current.FragmentShader.Uniforms.FogLut[entry][component], alpha);
         }
     }
+    for (size_t component = 0; component < 4U; ++component) {
+        fragmentUniforms.Lighting.GlobalAmbient[component] = InterpolateFinite(
+            previous.FragmentShader.Uniforms.Lighting.GlobalAmbient[component],
+            current.FragmentShader.Uniforms.Lighting.GlobalAmbient[component], alpha);
+    }
+    const auto interpolateLightArray = [&](auto member) {
+        auto& output = fragmentUniforms.Lighting.*member;
+        const auto& from = previous.FragmentShader.Uniforms.Lighting.*member;
+        const auto& to = current.FragmentShader.Uniforms.Lighting.*member;
+        for (size_t light = 0; light < output.size(); ++light) {
+            for (size_t component = 0; component < 4U; ++component) {
+                output[light][component] = InterpolateFinite(
+                    from[light][component], to[light][component], alpha);
+            }
+        }
+    };
+    interpolateLightArray(&Oot3dPicaFragmentLightingUniforms::Specular0);
+    interpolateLightArray(&Oot3dPicaFragmentLightingUniforms::Specular1);
+    interpolateLightArray(&Oot3dPicaFragmentLightingUniforms::Diffuse);
+    interpolateLightArray(&Oot3dPicaFragmentLightingUniforms::Ambient);
+    interpolateLightArray(&Oot3dPicaFragmentLightingUniforms::Position);
+    interpolateLightArray(&Oot3dPicaFragmentLightingUniforms::SpotDirection);
+    interpolateLightArray(&Oot3dPicaFragmentLightingUniforms::Attenuation);
 }

 void InterpolateVisualViewport(
diff --git a/tools/oot3d/native_game_runtime/oot3d_native_pica_vulkan_bridge.cpp b/tools/oot3d/native_game_runtime/oot3d_native_pica_vulkan_bridge.cpp
index 480dee9aa..d9fcc5062 100644
--- a/tools/oot3d/native_game_runtime/oot3d_native_pica_vulkan_bridge.cpp
+++ b/tools/oot3d/native_game_runtime/oot3d_native_pica_vulkan_bridge.cpp
@@ -3,6 +3,7 @@
 #include <algorithm>
 #include <array>
 #include <cstring>
+#include <vector>

 namespace Oot3dNativeGame {
 namespace {
@@ -12,11 +13,25 @@ constexpr size_t kPackedVertexFloatOffset =
     kPackedVertexIntegerOffset + 4U * 4U * sizeof(uint32_t);
 constexpr size_t kPackedVertexUniformSize =
     kPackedVertexFloatOffset + 96U * 4U * sizeof(float);
-constexpr size_t kPackedFragmentFogColorOffset = 128U;
+constexpr size_t kPackedFragmentShadowTextureBiasOffset = 128U;
+constexpr size_t kPackedFragmentShadowOrthographicOffset = 132U;
+constexpr size_t kPackedFragmentShadowBiasConstantOffset = 136U;
+constexpr size_t kPackedFragmentShadowBiasLinearOffset = 140U;
+constexpr size_t kPackedFragmentFogColorOffset = 144U;
 constexpr size_t kPackedFragmentFogLutOffset =
     kPackedFragmentFogColorOffset + 4U * sizeof(float);
-constexpr size_t kPackedFragmentUniformSize =
+constexpr size_t kPackedFragmentBaseUniformSize =
     kPackedFragmentFogLutOffset + 64U * 4U * sizeof(float);
+constexpr size_t kPackedFragmentLightingUniformSize =
+    kPackedFragmentBaseUniformSize +
+    16U + 7U * 8U * 4U * sizeof(float) +
+    kOot3dPicaPackedLightingLutCount * 256U * sizeof(uint32_t);
+constexpr size_t kPackedFragmentLightingGlobalOffset =
+    kPackedFragmentBaseUniformSize;
+constexpr size_t kPackedFragmentLightingArraysOffset =
+    kPackedFragmentLightingGlobalOffset + 4U * sizeof(float);
+constexpr size_t kPackedFragmentLightingLutOffset =
+    kPackedFragmentLightingArraysOffset + 7U * 8U * 4U * sizeof(float);

 template <typename Destination, typename Source>
 Destination SameValueEnum(Source source) {
@@ -39,7 +54,7 @@ std::array<uint8_t, kPackedVertexUniformSize> PackVertexUniforms(
     return bytes;
 }

-std::array<uint8_t, kPackedFragmentUniformSize> PackFragmentUniforms(
+std::vector<uint8_t> PackFragmentUniforms(
     const Oot3dPicaFragmentUniformState& uniforms,
     const Oot3dPicaViewportState& viewport) {
     constexpr size_t kConstantsOffset = 0U;
@@ -48,7 +63,9 @@ std::array<uint8_t, kPackedFragmentUniformSize> PackFragmentUniforms(
     constexpr size_t kDepthScaleOffset = kAlphaReferenceOffset + sizeof(int32_t);
     constexpr size_t kDepthOffsetOffset = kDepthScaleOffset + sizeof(float);
     constexpr size_t kWBufferingOffset = kDepthOffsetOffset + sizeof(float);
-    std::array<uint8_t, kPackedFragmentUniformSize> bytes{};
+    std::vector<uint8_t> bytes(
+        uniforms.LightingEnabled ? kPackedFragmentLightingUniformSize
+                                 : kPackedFragmentBaseUniformSize);
     std::memcpy(bytes.data() + kConstantsOffset, uniforms.TevConstants.data(),
                 sizeof(uniforms.TevConstants));
     std::memcpy(bytes.data() + kCombinerOffset,
@@ -63,6 +80,18 @@ std::array<uint8_t, kPackedFragmentUniformSize> PackFragmentUniforms(
     const int32_t wBuffering = viewport.ZBuffering ? 0 : 1;
     std::memcpy(bytes.data() + kWBufferingOffset, &wBuffering,
                 sizeof(wBuffering));
+    std::memcpy(bytes.data() + kPackedFragmentShadowTextureBiasOffset,
+                &uniforms.ShadowTextureBias,
+                sizeof(uniforms.ShadowTextureBias));
+    std::memcpy(bytes.data() + kPackedFragmentShadowOrthographicOffset,
+                &uniforms.ShadowOrthographic,
+                sizeof(uniforms.ShadowOrthographic));
+    std::memcpy(bytes.data() + kPackedFragmentShadowBiasConstantOffset,
+                &uniforms.ShadowBiasConstant,
+                sizeof(uniforms.ShadowBiasConstant));
+    std::memcpy(bytes.data() + kPackedFragmentShadowBiasLinearOffset,
+                &uniforms.ShadowBiasLinear,
+                sizeof(uniforms.ShadowBiasLinear));
     std::memcpy(bytes.data() + kPackedFragmentFogColorOffset,
                 uniforms.FogColor.data(),
                 sizeof(uniforms.FogColor));
@@ -76,6 +105,34 @@ std::array<uint8_t, kPackedFragmentUniformSize> PackFragmentUniforms(
                         pair * sizeof(packed),
                     packed.data(), sizeof(packed));
     }
+    if (!uniforms.LightingEnabled) {
+        return bytes;
+    }
+    std::memcpy(bytes.data() + kPackedFragmentLightingGlobalOffset,
+                uniforms.Lighting.GlobalAmbient.data(),
+                sizeof(uniforms.Lighting.GlobalAmbient));
+    size_t lightingOffset = kPackedFragmentLightingArraysOffset;
+    const auto copyLightArray = [&](const auto& values) {
+        std::memcpy(bytes.data() + lightingOffset, values.data(), sizeof(values));
+        lightingOffset += sizeof(values);
+    };
+    copyLightArray(uniforms.Lighting.Specular0);
+    copyLightArray(uniforms.Lighting.Specular1);
+    copyLightArray(uniforms.Lighting.Diffuse);
+    copyLightArray(uniforms.Lighting.Ambient);
+    copyLightArray(uniforms.Lighting.Position);
+    copyLightArray(uniforms.Lighting.SpotDirection);
+    copyLightArray(uniforms.Lighting.Attenuation);
+    if (uniforms.Lighting.LutTables != nullptr) {
+        for (size_t slot = 0;
+             slot < kOot3dPicaPackedLightingLutTables.size(); ++slot) {
+            const auto& table = (*uniforms.Lighting.LutTables)
+                [kOot3dPicaPackedLightingLutTables[slot]];
+            std::memcpy(bytes.data() + kPackedFragmentLightingLutOffset +
+                            slot * table.size() * sizeof(uint32_t),
+                        table.data(), table.size() * sizeof(uint32_t));
+        }
+    }
     return bytes;
 }

@@ -162,6 +219,14 @@ bool SubmitOot3dPicaVulkanDrawPlan(
     draw.VertexAttributes =
         std::span<const Fast::GfxNativePicaVertexAttributeView>(
             attributes.data(), attributeCount);
+    if (plan.State.VertexInput.AttributeCount > 0U) {
+        draw.PositionAttributeLocation =
+            plan.State.ShaderInterface.InputRegisterByAttribute[0];
+    }
+    if (plan.State.VertexInput.AttributeCount > 1U) {
+        draw.TexCoord0AttributeLocation =
+            plan.State.ShaderInterface.InputRegisterByAttribute[1];
+    }
     draw.IndexBytes = plan.IndexBytes;
     draw.Indexed = plan.Indexed;
     draw.IndicesAre16Bit = plan.IndicesAre16Bit;
@@ -179,6 +244,7 @@ bool SubmitOot3dPicaVulkanDrawPlan(
     draw.ViewportHeight = viewport.HalfHeight * 2.0F;
     draw.DepthRange = viewport.DepthRange;
     draw.NearPlane = viewport.NearPlane;
+    draw.WBuffering = !viewport.ZBuffering;
     draw.FramebufferFlipped = plan.State.Framebuffer.Flipped;
     draw.FramebufferWidth = plan.State.Framebuffer.Width;
     draw.FramebufferHeight = plan.State.Framebuffer.Height;
diff --git a/tools/oot3d/native_pica_frontend/oot3d_native_pica_fragment_shader_gen.cpp b/tools/oot3d/native_pica_frontend/oot3d_native_pica_fragment_shader_gen.cpp
index 17d98f9b7..1add85577 100644
--- a/tools/oot3d/native_pica_frontend/oot3d_native_pica_fragment_shader_gen.cpp
+++ b/tools/oot3d/native_pica_frontend/oot3d_native_pica_fragment_shader_gen.cpp
@@ -1,5 +1,7 @@
 #include "oot3d_native_pica_fragment_shader_gen.h"

+#include <cmath>
+#include <limits>
 #include <sstream>

 namespace Oot3dNativeGame {
@@ -32,6 +34,22 @@ std::array<float, 4> DecodeColor(uint32_t value) {
     };
 }

+float DecodeFloat16(uint16_t value) {
+    const float sign = (value & 0x8000U) != 0U ? -1.0F : 1.0F;
+    const uint32_t exponent = (value >> 10U) & 0x1FU;
+    const uint32_t fraction = value & 0x3FFU;
+    if (exponent == 0x1FU) {
+        return fraction == 0U
+                   ? sign * std::numeric_limits<float>::infinity()
+                   : std::numeric_limits<float>::quiet_NaN();
+    }
+    if (exponent == 0U) {
+        return sign * std::ldexp(static_cast<float>(fraction), -24);
+    }
+    return sign * std::ldexp(1.0F + static_cast<float>(fraction) / 1024.0F,
+                             static_cast<int>(exponent) - 15);
+}
+
 std::string SourceExpression(uint8_t source, size_t stage,
                              const Oot3dPicaDecodedDrawState& state,
                              bool& supported) {
@@ -57,7 +75,7 @@ std::string SourceExpression(uint8_t source, size_t stage,
                    "pica_texcoord0_w))";
         }
         if (texture == 0U && state.Textures[texture].Type == 5U) {
-            return "vec4(0.0)";
+            return "oot3d_sample_shadow2d(pica_texcoord0, pica_texcoord0_w)";
         }
         if (texture == 0U && state.Textures[texture].Type != 0U) {
             supported = false;
@@ -84,6 +102,65 @@ std::string SourceExpression(uint8_t source, size_t stage,
     }
 }

+std::string Shadow2dHelpers(const Oot3dPicaDecodedDrawState& state) {
+    if (!state.Textures[0].Enabled || state.Textures[0].Type != 5U)
+        return {};
+    return R"glsl(
+float oot3d_compare_shadow2d(uint pixel, uint z) {
+    uint depth24 = pixel >> 8u;
+    uint penumbra = pixel & 0xffu;
+    return depth24 <= z ? 0.0 : float(penumbra) * (1.0 / 255.0);
+}
+
+float oot3d_shadow2d_tap(ivec2 coordinate, uint z) {
+    ivec2 size = textureSize(pica_texture0, 0);
+    if (any(lessThan(coordinate, ivec2(0))) ||
+        any(greaterThanEqual(coordinate, size))) return 1.0;
+    return oot3d_compare_shadow2d(
+        texelFetch(pica_texture0, coordinate, 0).x, z);
+}
+vec4 oot3d_sample_shadow2d(vec2 uv, float w) {
+    if (fragment_uniforms.shadow_orthographic == 0) uv /= w;
+    uint z = uint(max(0, int(min(abs(w), 1.0) * 16777215.0) -
+                          fragment_uniforms.shadow_texture_bias));
+    vec2 coordinate = vec2(textureSize(pica_texture0, 0)) * uv - vec2(0.5);
+    ivec2 base = ivec2(floor(coordinate));
+    vec2 factor = fract(coordinate);
+    vec4 samples = vec4(
+        oot3d_shadow2d_tap(base, z),
+        oot3d_shadow2d_tap(base + ivec2(1, 0), z),
+        oot3d_shadow2d_tap(base + ivec2(0, 1), z),
+        oot3d_shadow2d_tap(base + ivec2(1, 1), z));
+    vec2 vertical = mix(samples.xy, samples.zw, factor.yy);
+    return vec4(mix(vertical.x, vertical.y, factor.x));
+}
+)glsl";
+}
+
+std::string ShadowWriteHelpers(const Oot3dPicaDecodedDrawState& state) {
+    if (state.OutputMerger.FragmentOperationMode != 3U) return {};
+    return R"glsl(
+uint oot3d_encode_shadow(uvec2 pixel) {
+    return (pixel.x << 8u) | pixel.y;
+}
+uint oot3d_update_shadow(uint pixel, uint depth24, uint penumbra8) {
+    uvec2 reference = uvec2(pixel >> 8u, pixel & 0xffu);
+    if (depth24 < reference.x) {
+        if (penumbra8 == 0u) {
+            reference.x = depth24;
+        } else {
+            float divisor = fragment_uniforms.shadow_bias_constant +
+                fragment_uniforms.shadow_bias_linear *
+                float(depth24) / float(reference.x);
+            penumbra8 = uint(float(penumbra8) / divisor);
+            reference.y = min(penumbra8, reference.y);
+        }
+    }
+    return oot3d_encode_shadow(reference);
+}
+)glsl";
+}
+
 std::string ColorModifier(const std::string& source, uint8_t modifier,
                           bool& supported) {
     switch (modifier) {
@@ -227,6 +304,7 @@ uint64_t ComputeOot3dPicaFragmentShaderStateKey(
     key = HashWord(key, packet.Registers[0x08FU]);
     key = HashWord(key, packet.Registers[0x0E0U]);
     key = HashWord(key, packet.Registers[0x104U] & 0x71U);
+    key = HashWord(key, packet.Registers[0x100U] & 3U);
     for (uint16_t base : kTevStageRegisters) {
         key = HashWord(key, packet.Registers[base]);
         key = HashWord(key, packet.Registers[base + 1U]);
@@ -247,6 +325,14 @@ Oot3dPicaFragmentUniformState BuildOot3dPicaFragmentUniformState(
     uniforms.CombinerBufferColor = DecodeColor(packet.Registers[0x0FDU]);
     uniforms.AlphaReference =
         static_cast<int32_t>((packet.Registers[0x104U] >> 8U) & 0xFFU);
+    uniforms.ShadowTextureBias =
+        static_cast<int32_t>((packet.Registers[0x08BU] >> 1U) & 0x7FFFFFU);
+    uniforms.ShadowOrthographic =
+        static_cast<int32_t>(packet.Registers[0x08BU] & 1U);
+    uniforms.ShadowBiasConstant = DecodeFloat16(
+        static_cast<uint16_t>(packet.Registers[0x130U] & 0xFFFFU));
+    uniforms.ShadowBiasLinear = DecodeFloat16(
+        static_cast<uint16_t>(packet.Registers[0x130U] >> 16U));
     uniforms.FogColor = DecodeColor(packet.Registers[0x0E1U]);
     for (size_t index = 0; index < packet.FogLut.size(); ++index) {
         const uint32_t raw = packet.FogLut[index];
@@ -268,8 +354,8 @@ bool GenerateOot3dPicaFragmentShader(
     const Oot3dPicaDecodedDrawState& state,
     Oot3dPicaGeneratedFragmentShader& shader, std::string* error) {
     shader = {};
-    if ((packet.Registers[0x08FU] & 1U) != 0U) {
-        SetError(error, "PICA fragment lighting shader path is not connected");
+    Oot3dPicaFragmentLightingShader lighting;
+    if (!BuildOot3dPicaFragmentLightingShader(packet, state, lighting, error)) {
         return false;
     }
     const uint32_t fogMode = packet.Registers[0x0E0U] & 7U;
@@ -288,10 +374,14 @@ bool GenerateOot3dPicaFragmentShader(
               "layout(location=3) in vec2 pica_texcoord2;\n"
               "layout(location=4) in float pica_texcoord0_w;\n"
               "layout(location=5) in vec4 pica_normquat;\n"
-              "layout(location=6) in vec3 pica_view;\n"
-              "layout(set=0,binding=1) uniform sampler2D pica_texture0;\n"
+              "layout(location=6) in vec3 pica_view;\n";
+    source << (state.Textures[0].Type == 5U
+                   ? "layout(set=0,binding=1) uniform usampler2D pica_texture0;\n"
+                   : "layout(set=0,binding=1) uniform sampler2D pica_texture0;\n")
+           <<
               "layout(set=0,binding=2) uniform sampler2D pica_texture1;\n"
               "layout(set=0,binding=3) uniform sampler2D pica_texture2;\n"
+              "layout(set=0,binding=5,r32ui) uniform uimage2D pica_shadow_buffer;\n"
               "layout(set=0,binding=4,std140) uniform PicaFragmentUniforms {\n"
               "    vec4 tev_constants[6];\n"
               "    vec4 combiner_buffer_color;\n"
@@ -299,19 +389,33 @@ bool GenerateOot3dPicaFragmentShader(
               "    float depth_scale;\n"
               "    float depth_offset;\n"
               "    int w_buffering;\n"
+              "    int shadow_texture_bias;\n"
+              "    int shadow_orthographic;\n"
+              "    float shadow_bias_constant;\n"
+              "    float shadow_bias_linear;\n"
               "    vec4 fog_color;\n"
-              "    vec4 fog_lut[64];\n"
-              "} fragment_uniforms;\n"
+              "    vec4 fog_lut[64];\n";
+    source << lighting.UniformDeclarations
+           << "} fragment_uniforms;\n"
               "layout(location=0) out vec4 pica_color;\n"
+              "layout(location=1) out vec4 pica_normal_guide;\n"
+              "layout(location=2) out vec4 pica_material_guide;\n"
               "vec3 byteround3(vec3 value) { return floor(value * 255.0 + 0.5) / 255.0; }\n"
-              "float byteround1(float value) { return floor(value * 255.0 + 0.5) / 255.0; }\n"
+              "float byteround1(float value) { return floor(value * 255.0 + 0.5) / 255.0; }\n";
+    source << lighting.Helpers << Shadow2dHelpers(state) <<
+                  ShadowWriteHelpers(state)
+           <<
               "void main() {\n"
               "    vec4 rounded_primary_color = vec4(byteround3(pica_primary_color.rgb), byteround1(pica_primary_color.a));\n"
               "    vec4 primary_fragment_color = vec4(0.0);\n"
               "    vec4 secondary_fragment_color = vec4(0.0);\n"
               "    vec4 combiner_buffer = vec4(0.0);\n"
               "    vec4 next_combiner_buffer = fragment_uniforms.combiner_buffer_color;\n"
-              "    vec4 combiner_output = vec4(0.0);\n";
+              "    vec4 combiner_output = vec4(0.0);\n"
+              "    float oot3d_normquat_length = length(pica_normquat);\n"
+              "    vec4 oot3d_guide_quat = oot3d_normquat_length > 0.000001 ? pica_normquat / oot3d_normquat_length : vec4(0.0, 0.0, 0.0, 1.0);\n"
+              "    vec3 oot3d_normal_guide = normalize(vec3(2.0 * (oot3d_guide_quat.x * oot3d_guide_quat.z + oot3d_guide_quat.w * oot3d_guide_quat.y), 2.0 * (oot3d_guide_quat.y * oot3d_guide_quat.z - oot3d_guide_quat.w * oot3d_guide_quat.x), 1.0 - 2.0 * (oot3d_guide_quat.x * oot3d_guide_quat.x + oot3d_guide_quat.y * oot3d_guide_quat.y)));\n";
+    source << lighting.Body;

     bool supported = true;
     for (size_t stage = 0; stage < kTevStageRegisters.size(); ++stage) {
@@ -417,12 +521,46 @@ bool GenerateOot3dPicaFragmentShader(
                   "    float fog_factor = clamp(fog_entry.x + fog_entry.y * (fog_index - fog_i), 0.0, 1.0);\n"
                   "    combiner_output.rgb = mix(fragment_uniforms.fog_color.rgb, combiner_output.rgb, fog_factor);\n";
     }
-    source << "    gl_FragDepth = pica_depth;\n"
-              "    pica_color = vec4(byteround3(combiner_output.rgb), byteround1(combiner_output.a));\n"
-              "}\n";
+    source << "    gl_FragDepth = pica_depth;\n";
+    if (state.OutputMerger.FragmentOperationMode == 3U) {
+        source <<
+            "    uint shadow_depth = uint(clamp(pica_depth, 0.0, 1.0) * 16777215.0);\n"
+            "    uint shadow_penumbra = uint(combiner_output.g * 255.0);\n"
+            "    ivec2 shadow_coordinate = ivec2(gl_FragCoord.xy);\n"
+            "    uint shadow_old = imageLoad(pica_shadow_buffer, shadow_coordinate).x;\n"
+            "    uint shadow_new;\n"
+            "    uint shadow_observed;\n"
+            "    do {\n"
+            "        shadow_new = oot3d_update_shadow(shadow_old, shadow_depth, shadow_penumbra);\n"
+            "        shadow_observed = imageAtomicCompSwap(pica_shadow_buffer, shadow_coordinate, shadow_old, shadow_new);\n"
+            "        if (shadow_observed == shadow_old) break;\n"
+            "        shadow_old = shadow_observed;\n"
+            "    } while (true);\n"
+            "    pica_color = vec4(0.0);\n"
+            "    pica_normal_guide = vec4(0.5, 0.5, 1.0, 0.0);\n"
+            "    pica_material_guide = vec4(0.0);\n";
+    } else {
+        source <<
+            "    pica_color = vec4(byteround3(combiner_output.rgb), byteround1(combiner_output.a));\n"
+            "    pica_normal_guide = vec4(oot3d_normal_guide * 0.5 + 0.5, 1.0);\n";
+        if (lighting.Enabled) {
+            source <<
+                "    float oot3d_specular_signal = clamp(max(max(secondary_fragment_color.r, secondary_fragment_color.g), secondary_fragment_color.b) * 4.0, 0.0, 1.0);\n"
+                "    pica_material_guide = vec4(oot3d_specular_signal, clamp(1.0 - oot3d_specular_signal * 0.75, 0.08, 1.0), 1.0, 0.0);\n";
+        } else {
+            source << "    pica_material_guide = vec4(0.0);\n";
+        }
+    }
+    source << "}\n";

     shader.Uniforms = BuildOot3dPicaFragmentUniformState(packet);
+    shader.Uniforms.LightingEnabled = lighting.Enabled;
+    shader.Uniforms.Lighting = lighting.Uniforms;
     shader.StateKey = ComputeOot3dPicaFragmentShaderStateKey(packet, state);
+    shader.StateKey = HashWord(shader.StateKey,
+                               static_cast<uint32_t>(lighting.StructuralKey));
+    shader.StateKey = HashWord(
+        shader.StateKey, static_cast<uint32_t>(lighting.StructuralKey >> 32U));
     shader.Source = source.str();
     return true;
 }
diff --git a/tools/oot3d/native_pica_frontend/oot3d_native_pica_fragment_shader_gen.h b/tools/oot3d/native_pica_frontend/oot3d_native_pica_fragment_shader_gen.h
index 0344511c4..dbc348746 100644
--- a/tools/oot3d/native_pica_frontend/oot3d_native_pica_fragment_shader_gen.h
+++ b/tools/oot3d/native_pica_frontend/oot3d_native_pica_fragment_shader_gen.h
@@ -1,6 +1,7 @@
 #pragma once

 #include "oot3d_native_pica_draw_state.h"
+#include "oot3d_native_pica_fragment_lighting.h"

 #include <array>
 #include <cstdint>
@@ -9,11 +10,17 @@
 namespace Oot3dNativeGame {

 struct Oot3dPicaFragmentUniformState {
+    bool LightingEnabled = false;
     std::array<std::array<float, 4>, 6> TevConstants{};
     std::array<float, 4> CombinerBufferColor{};
     int32_t AlphaReference = 0;
+    int32_t ShadowTextureBias = 0;
+    int32_t ShadowOrthographic = 0;
+    float ShadowBiasConstant = 0.0F;
+    float ShadowBiasLinear = 0.0F;
     std::array<float, 4> FogColor{};
     std::array<std::array<float, 2>, 128> FogLut{};
+    Oot3dPicaFragmentLightingUniforms Lighting;
 };

 struct Oot3dPicaGeneratedFragmentShader {
