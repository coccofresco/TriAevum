# Alpha 2 Publication Preparation

Status: **draft, not published and not a qualified final package**.
Version: `v0.6.0-alpha.2`; baseline: `v0.6.0-alpha.1c`.
Release-note proposal: [essential changelog](v0.6.0-alpha.2.md).
Do not change the live README download link until actual assets are available.

## Intended Artifacts

| Artifact | Contract | Current state |
| --- | --- | --- |
| `TriAevum-v0.6.0-alpha.2-Windows-x64.zip` | Portable Forge, current runtime, precompiled title, bundled portable shaders, source and notices | Final archive not built |
| `TriAevum-v0.6.0-alpha.2-Linux-x86_64.flatpak` | One Linux/Steam Deck target, immutable package plus user-private activation/cache | Final public bundle not built |
| `SHA256SUMS.txt` | Hash the final uploaded bytes, including source archives if separate | Generate after archive audit |
| Corresponding source archives | Exact runtime/Forge and translated title identities, donor notices | Must match the final builds |

Do not publish a copied development installation or the desktop's private test
Flatpak. Those contain private qualification artifacts or obsolete publication
manifests. Preserve user installs; qualify updates on separate copies.

## Verified Inputs, Not Final-Package Qualification

The shared automatic corpus binding is committed in `e0887e5` and documented in
[bundled Forge qualification](../TRIAEVUM_BUNDLED_FORGE_SHADER_CORPUS.md).
Both frozen Forge workers prepared 22 renderer modules and 588 GPU pipelines
from empty caches. Both games loaded 1,039 portable modules and accepted their
locally prepared GPU cache. Each second mounted replay compiled zero modules.
The first replay still compiled 32 PICA variants and two renderer passes.

Windows qualification used runtime SHA256
`6a1f9454984b86f9b98eb8682a8713ed31d2142a0b3b0dfe8f874b0b559dae24`
from `J:/TriAevum-verify-20260910/runtime`, with embedded source identity
`55d17e46bb429ad572763e82f711a87838a98841`. The similarly named `package` folder
contains an older `c7f9686` runtime and must not supply the new executable.
Linux runtime SHA256:
`629afe741ca4ae262b5ed60c8385381ba62ee7becabc474c27b0702cfc88e5b6`.
Linux title SHA256:
`97de67f0e595375761661831fa65c4e14f9a70eee52849a4a55d89459d79be15`.

## Publication Boundary

The maintainer has requested the portable shader corpus for all Forge users.
The automatic permission reviewer nevertheless rejected the attempted audit
change because the supplied AGENTS invariant still categorically forbids
game-derived shader-cache payloads in public packages. No patch was applied
and no alternate execution path was used to bypass that rejection.

The maintainer subsequently explicitly confirmed the amendment: allow only the
catalogued portable shader programs and observed pipeline recipes, not GPU
driver caches, ROMs, extracted assets, saves, captures or mod archives. The
exception is represented in the publisher and its audit, not hidden in
the frozen executable. A runtime cache belongs to each user's installation.

## Remaining Execution Order

1. Test the amended boundary: exact-role, catalog, schema, integrity
   and unclaimed-data rejection for the shader corpus. Keep all unrelated
   artifact prohibitions intact.
2. Freeze a source revision. Reconfigure/rebuild only affected host/Forge
   artifacts, reusing the qualified title and its exact corresponding source.
   Do not claim a new source revision for an unchanged old executable.
3. Produce fresh allowlist-built Windows and Linux inventories. Include the
   shared corpus binder for every supported title recipe and platform-local
   preparation helpers. Retain PR authorship and required source/notices.
4. Audit the staged files and extracted final archives. Qualify the exact final
   Forge-to-game install and update paths on both hosts. Keep clean-machine
   Windows and physical Steam Deck limitations explicit; do not fabricate gates.
5. Produce checksums and a GitHub draft prerelease with the agreed notes. No
   tag, upload, public release or README download-link switch has occurred yet.

## Attribution Scope

Sion / `999sian`: #6 Linux infrastructure, #14 display feedback, #11 selective
outline/preparation integration, #13 product item hooks, #18 ocarina evidence
and adapted behavior. PR #15 informed review but its threaded game loop was
**not merged** and must not be credited as a shipped performance feature.
Pablo Souza: selective full-drawable readback from #16, not a macOS release.
rlgcarrot: original TopScreen author; distinguish our reimplementation from
redistributing the original mod. Preserve SoH, Azahar, NRI and other notices.

The exact original SHAs and adopted scopes remain in
[the contribution register](../TRIAEVUM_CONTRIBUTIONS.md).
